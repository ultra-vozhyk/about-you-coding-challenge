describe("<Filter />", () => {
  it("should render correctly, when there is no selection", () => {});

  it("should render correctly, when there are selected items", () => {});

  it("should call `onReset` callback, after reset button clicked", () => {});

  it("should call `onChange` callback, after checkbox click", () => {});
});
