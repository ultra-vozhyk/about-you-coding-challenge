describe("<ProductsContext />", () => {
  it("should return correct product list", () => {});

  it("should call `refresh` with delay", () => {});

  it("should call `refresh` without delay, if `force` parameter is provided", () => {});
});
